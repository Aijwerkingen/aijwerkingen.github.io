# AIjwerkingen — example robots.txt
# Replace https://example.org with the real canonical origin.
# AI-crawler policy is a DECISION (spec §11.6): the block below ALLOWS AI
# crawlers, which is usually desirable for a public-interest information site that
# wants to be the cited authority. If the organisation declines AI use, flip the
# relevant Disallow lines and record the decision as an ADR. Do NOT also block
# these at the CDN/WAF layer, or the policy here is meaningless.
#
# NOTE (P1-5): this static file is superseded by src/app/robots.ts once that
# lands — decide then whether this seed should still exist (see PENDING-FIXES.md P1-15).

User-agent: *
Allow: /
# Non-public / utility routes:
Disallow: /api/
Disallow: /*?*preview=
Disallow: /thank-you

# Explicitly welcome major AI answer-engine crawlers (decision: allow)
User-agent: GPTBot
Allow: /

User-agent: ClaudeBot
Allow: /

User-agent: PerplexityBot
Allow: /

User-agent: Google-Extended
Allow: /

Sitemap: https://example.org/sitemap.xml
